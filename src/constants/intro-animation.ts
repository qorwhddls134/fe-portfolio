/** Intro 진입 fade-up 애니메이션 타이밍(ms) 상수 */

export const INTRO_FADE_UP_DURATION_MS = 500;

/** tagline — 애니메이션 블록 중 첫 순서 */
export const INTRO_TAGLINE_DELAY_MS = 0;

/** h1 글자별 등장 시작 시점 (tagline 직후) */
export const INTRO_H1_BASE_DELAY_MS = INTRO_FADE_UP_DURATION_MS;

/** h1 글자 간 stagger */
export const INTRO_LETTER_STAGGER_MS = 80;

/** 마지막 글자 등장 후 description·버튼 시작까지 간격 */
export const INTRO_BLOCK_GAP_MS = 200;
